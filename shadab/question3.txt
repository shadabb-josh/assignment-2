Question - Under what conditions will Git consider a file that has been deleted as being renamed, and what criteria does Git use to determine that a new file is a renamed version of the deleted file?


Answer - if the file deleted and new file contain same content, simlarity index > 50% is required, it used levenshtein algorithm